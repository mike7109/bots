"""Delivery channels. Each transport implements `dispatch(event, rule, rendered, defaults)`.

Add a channel = add a module here and register it in the bot's wiring; the
engine and templates don't change.
"""
