📅 <b>Дедлайн завтра</b> · {{ extra.date | ru_date }} · {{ extra.total }}
<ul>
{% for it in extra["items"] %}
<li><a href="{{ it.url | safe_url }}">#{{ it.iid }}</a> {{ it.title | truncate(70, true, '…', 0) }}{% if it.due %} — <i>{{ it.due | ru_date }}</i>{% endif %}{{ who(it.assignees) }}</li>
{% endfor %}
</ul>
