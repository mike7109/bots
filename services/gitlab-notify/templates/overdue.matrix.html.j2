⏰ <b>Просроченные задачи</b> · {{ extra.total }}
<ul>
{% for it in extra["items"] %}
<li><a href="{{ it.url | safe_url }}">#{{ it.iid }}</a> {{ it.title | truncate(70, true, '…', 0) }} — <i>был {{ it.due | ru_date }}</i></li>
{% endfor %}
</ul>
